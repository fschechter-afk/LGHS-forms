// School-wide defaults baked into the deployed site.

// The school's Apps Script web app URL (ends in /exec). When set, the short
// link  <site>#/chat  gets AI answers and school updates directly — no long
// encoded link needed. This URL is not a secret (every shared chatbox link
// already carries it); the AI key stays in Apps Script's Script Properties.
export const DEFAULT_CHAT_ENDPOINT = ''
